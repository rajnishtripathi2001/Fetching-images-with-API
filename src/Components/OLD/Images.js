{/*
import React from 'react'
import { IndividualIamges } from './IndividualIamges'

export default function Images({images}) {
    //console.log(image)
  return (
   images.map((image)=>(
    <IndividualIamges key={image.id} image={image}/>
   ))
  )
}

*/}